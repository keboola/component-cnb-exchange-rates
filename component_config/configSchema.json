{
  "type": "object",
  "title": "Extractor configuration",
  "required": [
    "current_as_today",
    "dates"
  ],
  "properties": {
    "current_as_today": {
      "type": "boolean",
      "format": "checkbox",
      "title": "Current rates as today's rates",
      "description": "Checking this box is recommended",
      "default": true,
      "propertyOrder": 100
    },
    "dates": {
      "type": "string",
      "title": "Dates",
      "enum": [
        "today",
        "today + yesterday",
        "week"
      ],
      "propertyOrder": 200,
      "default": "today + yesterday"
    }
  }
}